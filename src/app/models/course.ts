export class Course {
  constructor(public id:string, public course:string, public description:string, public image:string, public start_maxile_score:string, public end_maxile_score:string, public status_id:string, public prereq_course_id:string, public imageLink:string) {
  }
}